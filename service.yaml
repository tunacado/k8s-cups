apiVersion: v1
kind: Service
metadata:
  name: cups
  namespace: cups
  labels:
    app: cups
spec:
  ports:
  - name: "6631"
    port: 6631
    protocol: TCP
    targetPort: 631
  selector:
    app: cups
  type: ClusterIP
